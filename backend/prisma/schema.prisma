// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client"
  output = "../src/generated/prisma"
  moduleFormat = "cjs"
}

datasource db {
  provider = "postgresql"
}

model User {
  id        Int   @default(autoincrement()) @id
  email     String   @unique
  name      String
  password  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  projects  Project[]

  @@map("users")
}

model Project {
  id          Int   @default(autoincrement()) @id
  name        String
  description String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  userId      Int
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  environments Environment[]

  // Garantia que um usuário não pode ter dois projetos com o mesmo nome.
  @@unique([userId, name])
  @@map("projects")
}

model Environment {
  id          Int   @default(autoincrement()) @id
  name        String
  description String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  projectId   Int
  project     Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)

  featureFlags FeatureFlag[]

  // Garantindo que dentro de um projeto, não existam dois ambientes com o mesmo nome.
  @@unique([projectId, name])
  @@map("environments")
}

model FeatureFlag {
  id          Int   @default(autoincrement()) @id
  name        String
  description String?
  isActive    Boolean  @default(false)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  environmentId Int
  environment   Environment @relation(fields: [environmentId], references: [id], onDelete: Cascade)

  // Garantindo que dentro de um ambiente, não existam dois feature flags com o mesmo nome.
  @@unique([environmentId, name])
  @@map("feature_flags")
}
